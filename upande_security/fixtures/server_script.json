[
 {
  "allow_guest": 0,
  "api_method": null,
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Save",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-23 06:17:26.246433",
  "module": "Upande Security",
  "name": "Appointment Host WA Number",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": "Appointment",
  "script": "\nd = \"\".join(c for c in (doc.custom_meet_with_phone or \"\") if c.isdigit())\nif d.startswith(\"254\"):\n    pass\nelif d.startswith(\"0\"):\n    d = \"254\" + d[1:]\nelif len(d) == 9 and d[0] in (\"7\", \"1\"):\n    d = \"254\" + d\ndoc.custom_meet_with_wa = d\ndoc.custom_visit_purpose = (doc.customer_details or \"\").strip() or \"a visit\"\ndoc.custom_wa_link_suffix = (doc.name or \"\").replace(\" \", \"%20\")\n",
  "script_type": "DocType Event"
 },
 {
  "allow_guest": 0,
  "api_method": "getVisitorAppointment",
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Insert",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-23 06:18:48.172910",
  "module": "Upande Security",
  "name": "Fetch Visitor Appointment",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": null,
  "script": "try:\n    data = frappe.request.get_json()\n    query = data.get(\"query\")\n\n    if not query:\n        frappe.throw(\"Search criteria is required\")\n\n    # Search for the appointment across multiple fields\n    appointments = frappe.get_all(\n        \"Appointment\",\n        filters={\n            \"status\": [\"not in\", [\"Cancelled\", \"Closed\"]]\n        },\n        or_filters=[\n            [\"name\", \"like\", f\"%{query}%\"],\n            [\"customer_name\", \"like\", f\"%{query}%\"],\n            [\"customer_phone_number\", \"like\", f\"%{query}%\"]\n        ],\n        fields=[\n            \"name\", \n            \"customer_name\", \n            \"customer_phone_number\", \n            \"customer_details\", \n            \"status\",\n            \"scheduled_time\",\n            \"custom_meet_with\",\n            \"custom_reporting_status\",\n            \"custom_mode_of_transport\",\n            \"custom_vehicles_number_plate\",\n            \"custom_vehicles_colour\"\n        ],\n        limit=1\n    )\n\n    # Default response structure\n    result = {\n        \"has_appointment\": False,\n        \"status\": \"No Appointment Found\"\n    }\n\n    if appointments:\n        appt = appointments[0]\n        \n        # Resolve Host Name from Employee ID\n        host_id = appt.get(\"custom_meet_with\")\n        host_display_name = host_id\n        if host_id:\n            employee_name = frappe.db.get_value(\"Employee\", host_id, \"employee_name\")\n            if employee_name:\n                host_display_name = employee_name\n\n        # Mapping fields for the Flutter model\n        result = {\n            \"has_appointment\": True,\n            \"name\": appt.get(\"name\"),\n            \"visitor_name\": appt.get(\"customer_name\"),\n            \"id_no\": appt.get(\"name\"), \n            \"phone_number\": appt.get(\"customer_phone_number\"),\n            \"organization\": \"Visitor\", \n            \"host_name\": host_display_name,\n            \"scheduled_time\": appt.get(\"scheduled_time\"), # Raw datetime for Flutter to handle\n            \"purpose\": appt.get(\"customer_details\") or \"General Visit\",\n            \"transport_mode\": appt.get(\"custom_mode_of_transport\") or \"On Foot\",\n            \"vehicle_reg_no\": appt.get(\"custom_vehicles_number_plate\") or \"\",\n            \"vehicle_color\": appt.get(\"custom_vehicles_colour\") or \"\",\n            \"status\": appt.get(\"custom_reporting_status\") or appt.get(\"status\")\n        }\n\n    frappe.response['message'] = result\n\nexcept Exception as e:\n    frappe.log_error(\"Gate Check-in Search Error\")\n    frappe.response['message'] = {\n        \"has_appointment\": False,\n        \"status\": \"Error\",\n        \"message\": str(e)\n    }",
  "script_type": "API"
 },
 {
  "allow_guest": 0,
  "api_method": "approved_appointments",
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Insert",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-23 06:19:23.684229",
  "module": "Upande Security",
  "name": "Approved Appointments",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": null,
  "script": "try:\n    rows = frappe.db.sql(\n        \"\"\"\n        SELECT a.name, a.customer_name, a.customer_phone_number,\n               a.customer_details, a.scheduled_time, a.workflow_state,\n               a.custom_meet_with,\n               a.custom_check_in_time, a.custom_mode_of_transport,\n               a.custom_vehicles_number_plate, a.custom_vehicles_colour,\n               a.custom_number_of_passengers,\n               e.employee_name AS host_name\n        FROM `tabAppointment` a\n        LEFT JOIN `tabEmployee` e ON e.name = a.custom_meet_with\n        WHERE a.workflow_state IN ('Approved by Host', 'Visitor Checked In')\n        ORDER BY\n          CASE a.workflow_state\n            WHEN 'Visitor Checked In' THEN 0\n            ELSE 1\n          END,\n          a.custom_check_in_time DESC,\n          a.scheduled_time DESC\n        LIMIT 100\n        \"\"\",\n        as_dict=True,\n    )\n    result = []\n    for r in rows:\n        try:\n            rname = str(r[\"name\"]) if r[\"name\"] else \"\"\n        except (KeyError, TypeError):\n            rname = \"\"\n        try:\n            cname = str(r[\"customer_name\"]) if r[\"customer_name\"] else \"\"\n        except (KeyError, TypeError):\n            cname = \"\"\n        try:\n            phone = str(r[\"customer_phone_number\"]) if r[\"customer_phone_number\"] else \"\"\n        except (KeyError, TypeError):\n            phone = \"\"\n        try:\n            purpose = str(r[\"customer_details\"]) if r[\"customer_details\"] else \"\"\n        except (KeyError, TypeError):\n            purpose = \"\"\n        try:\n            sched = str(r[\"scheduled_time\"]) if r[\"scheduled_time\"] else \"\"\n        except (KeyError, TypeError):\n            sched = \"\"\n        try:\n            wf = str(r[\"workflow_state\"]) if r[\"workflow_state\"] else \"\"\n        except (KeyError, TypeError):\n            wf = \"\"\n        try:\n            host_id = str(r[\"custom_meet_with\"]) if r[\"custom_meet_with\"] else \"\"\n        except (KeyError, TypeError):\n            host_id = \"\"\n        try:\n            hname = str(r[\"host_name\"]) if r[\"host_name\"] else host_id\n        except (KeyError, TypeError):\n            hname = host_id\n        try:\n            check_in_time = str(r[\"custom_check_in_time\"]) if r[\"custom_check_in_time\"] else \"\"\n        except (KeyError, TypeError):\n            check_in_time = \"\"\n        try:\n            transport = str(r[\"custom_mode_of_transport\"]) if r[\"custom_mode_of_transport\"] else \"\"\n        except (KeyError, TypeError):\n            transport = \"\"\n        try:\n            plate = str(r[\"custom_vehicles_number_plate\"]) if r[\"custom_vehicles_number_plate\"] else \"\"\n        except (KeyError, TypeError):\n            plate = \"\"\n        try:\n            colour = str(r[\"custom_vehicles_colour\"]) if r[\"custom_vehicles_colour\"] else \"\"\n        except (KeyError, TypeError):\n            colour = \"\"\n        passengers = 0\n        try:\n            pv = r[\"custom_number_of_passengers\"]\n            if pv is not None:\n                passengers = int(pv)\n        except (KeyError, TypeError, ValueError):\n            passengers = 0\n        result.append({\n            \"name\": rname,\n            \"customer_name\": cname,\n            \"phone\": phone,\n            \"purpose\": purpose,\n            \"scheduled_time\": sched,\n            \"workflow_state\": wf,\n            \"host_id\": host_id,\n            \"host_name\": hname,\n            \"check_in_time\": check_in_time,\n            \"transport\": transport,\n            \"plate\": plate,\n            \"colour\": colour,\n            \"passengers\": passengers,\n        })\n    frappe.response[\"message\"] = result\nexcept Exception as e:\n    frappe.log_error(\"approved_appointments\", str(e))\n    frappe.response[\"message\"] = {\"error\": str(e)}\n",
  "script_type": "API"
 },
 {
  "allow_guest": 0,
  "api_method": "search_visitor_appointment",
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Insert",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-23 06:19:48.435170",
  "module": "Upande Security",
  "name": "Search Visitor Appointment",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": null,
  "script": "try:\n    data = {}\n    try:\n        data = frappe.request.get_json(silent=True) or {}\n    except Exception:\n        data = {}\n    if not data:\n        data = dict(frappe.form_dict or {})\n    query = str(data.get(\"query\") or \"\").strip()\n\n    if not query:\n        frappe.response[\"message\"] = {\"error\": \"query is required\"}\n    else:\n        today = frappe.utils.today()\n        like = \"%\" + query + \"%\"\n\n        rows = frappe.db.sql(\n            \"\"\"\n            SELECT a.name, a.customer_name, a.customer_phone_number,\n                   a.customer_email, a.custom_meet_with,\n                   e.employee_name AS host_name,\n                   a.scheduled_time, a.customer_details,\n                   a.custom_mode_of_transport,\n                   a.custom_vehicles_number_plate, a.custom_vehicles_colour,\n                   a.workflow_state, a.status, a.custom_reporting_status\n            FROM `tabAppointment` a\n            LEFT JOIN `tabEmployee` e ON e.name = a.custom_meet_with\n            WHERE DATE(a.scheduled_time) = %s\n              AND (a.customer_name LIKE %s\n                   OR a.customer_phone_number LIKE %s\n                   OR a.name LIKE %s)\n            ORDER BY a.scheduled_time DESC\n            LIMIT 1\n            \"\"\",\n            (today, like, like, like),\n            as_dict=True,\n        )\n\n        if rows:\n            r = rows[0]\n            frappe.response[\"message\"] = {\n                \"has_appointment\": True,\n                \"name\": r.name,\n                \"visitor_name\": r.customer_name or \"\",\n                \"id_no\": \"\",\n                \"phone_number\": r.customer_phone_number or \"\",\n                \"organization\": \"\",\n                \"host_id\": r.custom_meet_with or \"\",\n                \"host_name\": r.host_name or r.custom_meet_with or \"\",\n                \"scheduled_time\": str(r.scheduled_time) if r.scheduled_time else \"\",\n                \"purpose\": r.customer_details or \"\",\n                \"transport_mode\": r.custom_mode_of_transport or \"On Foot\",\n                \"vehicle_reg_no\": r.custom_vehicles_number_plate or \"\",\n                \"vehicle_color\": r.custom_vehicles_colour or \"\",\n                \"status\": r.workflow_state or r.status or \"\",\n                \"reporting_status\": r.custom_reporting_status or \"\",\n            }\n        else:\n            frappe.response[\"message\"] = {\n                \"has_appointment\": False,\n                \"query\": query,\n            }\nexcept Exception as e:\n    frappe.log_error(\"search_visitor_appointment\", str(e))\n    frappe.response[\"message\"] = {\"error\": str(e)}\n",
  "script_type": "API"
 },
 {
  "allow_guest": 0,
  "api_method": "get_appointment",
  "cron_format": null,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Server Script",
  "doctype_event": "Before Insert",
  "enable_rate_limit": 0,
  "event_frequency": "All",
  "modified": "2026-07-23 06:21:27.195475",
  "module": "Upande Security",
  "name": "Get Appointment",
  "rate_limit_count": 5,
  "rate_limit_seconds": 86400,
  "reference_doctype": null,
  "script": "try:\n    data = {}\n    try:\n        data = frappe.request.get_json(silent=True) or {}\n    except Exception:\n        data = {}\n    if not data:\n        data = dict(frappe.form_dict or {})\n\n    name = \"\"\n    try:\n        name = str(data[\"name\"] or \"\").strip()\n    except (KeyError, TypeError):\n        name = \"\"\n\n    if not name:\n        frappe.response[\"message\"] = {\"error\": \"name is required\"}\n    else:\n        row = frappe.db.sql(\n            \"\"\"\n            SELECT a.name, a.customer_name, a.customer_phone_number,\n                   a.customer_email, a.custom_meet_with,\n                   e.employee_name AS host_name,\n                   a.scheduled_time, a.customer_details,\n                   a.custom_mode_of_transport,\n                   a.custom_vehicles_number_plate, a.custom_vehicles_colour,\n                   a.workflow_state, a.status, a.custom_reporting_status,\n                   a.custom_check_in_time, a.custom_check_out_time\n            FROM `tabAppointment` a\n            LEFT JOIN `tabEmployee` e ON e.name = a.custom_meet_with\n            WHERE a.name = %s\n            LIMIT 1\n            \"\"\",\n            (name,),\n            as_dict=True,\n        )\n        if not row:\n            frappe.response[\"message\"] = {\"error\": \"Appointment \" + name + \" not found\"}\n        else:\n            r = row[0]\n            frappe.response[\"message\"] = {\n                \"name\": r.name,\n                \"customer_name\": r.customer_name or \"\",\n                \"customer_phone_number\": r.customer_phone_number or \"\",\n                \"customer_email\": r.customer_email or \"\",\n                \"custom_meet_with\": r.custom_meet_with or \"\",\n                \"host_name\": r.host_name or r.custom_meet_with or \"\",\n                \"scheduled_time\": str(r.scheduled_time) if r.scheduled_time else \"\",\n                \"customer_details\": r.customer_details or \"\",\n                \"custom_mode_of_transport\": r.custom_mode_of_transport or \"\",\n                \"custom_vehicles_number_plate\": r.custom_vehicles_number_plate or \"\",\n                \"custom_vehicles_colour\": r.custom_vehicles_colour or \"\",\n                \"workflow_state\": r.workflow_state or \"\",\n                \"status\": r.status or \"\",\n                \"custom_reporting_status\": r.custom_reporting_status or \"\",\n                \"custom_check_in_time\": str(r.custom_check_in_time) if r.custom_check_in_time else \"\",\n                \"custom_check_out_time\": str(r.custom_check_out_time) if r.custom_check_out_time else \"\",\n            }\nexcept Exception as e:\n    frappe.log_error(\"get_appointment\", str(e))\n    frappe.response[\"message\"] = {\"error\": str(e)}\n",
  "script_type": "API"
 }
]